import { createTheme } from "@mui/material";

export const movieTheme = createTheme({
    palette: {
        primary: {
            main: "#f00000", 
            
        },
        secondary: {
            main: "#ffffff"

        }
    }
}) 