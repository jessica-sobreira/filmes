import { createTheme } from "@mui/material";

export const movieTheme = createTheme({
    palette: {
        primary: {
            main: "#680e34"
        },
        secondary: {
            main: "#c21b12"
        }
    }
});